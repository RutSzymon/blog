<p><div class="ctitle"><%= params[:id] %></div><p>

<% @articles.each do |article| %>
<article>
    <p><strong><%= article.title %></strong></p>
    <p><%= article.summary %></p>
    <p><%= link_to "zobacz artykuł", tag_article_path(params[:id], article) %></p>
  </article>
<% end %>

<%= paginate @articles %>